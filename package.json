{
  "name": "loqwi",
  "private": true,
  "type": "module",
  "description": "Loqwi — Voice AI Teaching Assistant. Root package.json so /api/*.js (Vercel serverless functions) resolve as ESM and their dependencies (shared with server/) are installed where Vercel's function bundler can find them.",
  "dependencies": {
    "@upstash/ratelimit": "^2.0.8",
    "@upstash/redis": "^1.38.0"
  }
}
