{
  "name": "loqwi-server",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Backend for Loqwi — holds the Gemini API key server-side and proxies structured assistant calls.",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "lint": "eslint .",
    "format": "prettier --write .",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@upstash/ratelimit": "^2.0.8",
    "@upstash/redis": "^1.38.0",
    "express": "^4.19.2"
  },
  "devDependencies": {
    "@eslint/js": "^9.39.4",
    "eslint": "^9.39.4",
    "globals": "^17.7.0",
    "prettier": "^3.9.0"
  }
}
